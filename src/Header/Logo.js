import React from 'react';
import logo from "../logo.svg";

function Logo (props) {
    return (
        <div className="logotipe">
            <img src={logo} alt="logo.svg"/>
        </div>
    );
}

export default Logo ;